# Security and safety reports

For ordinary bugs, open a GitHub issue.

For a defect that could cause unsafe thermal behavior, unexpected EC writes, uncontrolled fan shutdown, or privilege-related security problems, clearly mark the report as **SAFETY** or **SECURITY** and avoid posting private machine identifiers.

Until the project reaches a stable control release, all fan-control functionality should be considered experimental.
