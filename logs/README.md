# Local telemetry logs

CSV captures are stored here by default and are ignored by Git.

Before sharing a log publicly, inspect it for unintended identifying information. v0.1 does not intentionally log serial numbers, usernames, file paths, IP addresses or machine GUIDs.
